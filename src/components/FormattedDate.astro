---
interface Props { date: Date; }
const { date } = Astro.props;
const text = date.toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric', timeZone: 'UTC' });
---
<time datetime={date.toISOString()}>{text}</time>
